# 复制为 .env 并按需修改
# PostgreSQL 连接串（见 services/docker-compose.yml）
DATABASE_URL="postgresql://aicom:aicom_dev@localhost:5432/aicom?schema=public"

# JWT 签名密钥，生产环境务必更换为随机长串
JWT_SECRET="dev-secret-change-me"

# 服务端口
PORT=3001
